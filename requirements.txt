# Dashboard / web deployment dependencies (Posit Connect Cloud installs these).
# The microphone listener needs additional packages — see requirements-listen.txt.
fastapi
uvicorn[standard]
jinja2
sqlalchemy
psycopg2-binary
python-dotenv
